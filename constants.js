const PORT = process.env.PORT || 3000;

const FRAME_RATE = 10;
const MIN_GRID_SIZE_X = 20;
const MIN_GRID_SIZE_Y = 20;
const CELL_SIZE = 16;
const LENGTH_CODE = 5;
const MIN_SNAKE_LENGTH = 2;

module.exports = {
    PORT,
    FRAME_RATE,
    MIN_GRID_SIZE_X,
    MIN_GRID_SIZE_Y,
    CELL_SIZE,
    LENGTH_CODE,
    MIN_SNAKE_LENGTH,
}